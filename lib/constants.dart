import 'dart:ui';

const kPrimaryColor = Color(0xff100B20);
//you don't have to use const Color(0xff100B20) after having declared the variable as const
const kTransitionDuration = Duration(milliseconds: 250);